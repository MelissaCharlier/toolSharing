<?php
$resultCopies = $this->$ListCopies;
foreach ($resultCopies as $objetCopie){
    var_dump($objetCopie);
    // A partir de l'objet utiliser les get pour obtenir un attribut
    var_dump ($objetCopie->getDetails());
    
}